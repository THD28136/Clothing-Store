@using System.Globalization
    @model MTKPM_Clothing_Store_web.Models.CheckoutViewModel

@{
    ViewData["Title"] = "Thanh toán";
    Layout = "~/Views/Shared/_CustomerLayout.cshtml";
    var vn = CultureInfo.GetCultureInfo("vi-VN");
}

<h2>Thanh toán</h2>

@if (!Model.Items.Any())
{
    <div class="alert alert-info">Giỏ hàng trống.</div>
}
else
{
    <div class="table-responsive mb-3">
        <table class="table">
            <thead>
                <tr>
                    <th>Sản phẩm</th>
                    <th>Đơn giá</th>
                    <th>Số lượng</th>
                    <th>Thành tiền</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var it in Model.Items)
                {
                    var subtotal = it.Product.Price * it.Quantity;
                    <tr>
                        <td>@it.Product.Name</td>
                        <td>@it.Product.Price.ToString("C0", vn)</td>
                        <td>@it.Quantity</td>
                        <td>@subtotal.ToString("C0", vn)</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

    <div class="mb-3">
        <h5>Tổng: @Model.Total.ToString("C0", vn)</h5>
    </div>

    <form asp-controller="Payments" asp-action="CheckoutPost" method="post">
        @Html.AntiForgeryToken()
        <div class="mb-3">
            <label class="form-label">Tên trên thẻ</label>
            <input class="form-control" name="cardName" required />
        </div>
        <div class="mb-3">
            <label class="form-label">Số thẻ</label>
            <input class="form-control" name="cardNumber" required />
        </div>
        <div class="d-flex gap-2">
            <button class="btn btn-primary" type="submit">Thanh toán</button>
            <a class="btn btn-outline-secondary" asp-controller="Cart" asp-action="Index">Quay lại giỏ hàng</a>
        </div>
    </form>
}